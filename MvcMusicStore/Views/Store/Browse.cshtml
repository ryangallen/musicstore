@model MvcMusicStore.Models.Genre
@{
    ViewBag.Title = "Browse";
}
<h1>@Model.Name</h1>
<ul>
    @foreach (var album in Model.Albums)
    {
        <li>@Html.ActionLink(album.Title, "Details", 
            new {id = album.AlbumId})</li>
    }
</ul>
